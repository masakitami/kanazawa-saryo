---
// import { getBlogs } from '../libs/microcms';
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
import { newtClient } from '../libs/newt';
import type { Article } from '../libs/newt';
const { items: articles } = await newtClient.getContents<Article>({
  appUid: 'blog',
  modelUid: 'article',
  query: {
    select: ['title', 'slug', 'body'],
  },
});
---

<Layout
  title="kanazawa saryo"
  description="金沢茶寮は自分の手で色彩をほどこした器でプレミアム日本茶を嗜むことが出来る体感型アートカフェです。"
  keyword="金沢、カフェ、金沢塗り、伝統工芸"
>
  <!-- メインビジュアルー -->
  <section>
    <div
      class="grid grid-cols-12 grid-rows-1 place-items-center gap-x-[max(6.6vw,1rem)] px-[max(4.8vw,1.5rem)]"
    >
      <div class="col-span-4 col-start-5">
        <figure>
          <img src="/images/mainvisual_1.png" alt="" />
        </figure>
      </div>
      <header class="col-span-2 col-start-1 align-middle">
        <h2 lang="en" class="w-fit">
          Unleash Your<br translate="no" aria-hidden="true" />Five Senses
        </h2>
      </header>
    </div>
  </section>
  <!-- /メインビジュアル -->
  <!-- <ul>
      {
        articles.map((article) => {
          return (
            <li>
              <a href={`articles/${article.slug}`}>{article.title}</a>
            </li>
          );
        })
      }
    </ul> -->
</Layout>

<script>
  console.log('記入可能');
</script>
